import {PackageCheck,ShoppingBag} from "lucide-react";
export type MemberPurchase={id:string;purchaseCode:string;serviceName:string;paymentStatus:string;purchaseStatus:string;ballotingStatus:string};
type Props={purchases:MemberPurchase[]};
export default function PurchasesCard({purchases}:Props){return <section className="rounded-3xl border border-white/10 bg-white/[0.035] p-6 xl:col-span-2"><div className="flex items-start justify-between"><div><h3 className="text-lg font-bold">My Purchases</h3><p className="mt-1 text-sm text-slate-500">Confirmed products and product-wise balloting status</p></div><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-500/15 text-emerald-300"><ShoppingBag className="h-5 w-5"/></div></div>{purchases.length===0?<div className="mt-8 rounded-3xl border border-dashed border-white/10 px-6 py-12 text-center"><PackageCheck className="mx-auto h-10 w-10 text-slate-600"/><h4 className="mt-4 font-semibold">No confirmed purchase yet</h4><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Complete KYC, select a product and upload the required payment. Admin verification ke baad purchase yahan show hogi.</p></div>:<div className="mt-6 space-y-4">{purchases.map(p=><article key={p.id} className="rounded-2xl border border-white/10 bg-slate-950/35 p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><p className="text-xs uppercase tracking-wider text-slate-600">{p.purchaseCode}</p><h4 className="mt-2 text-lg font-bold">{p.serviceName}</h4></div><span className="self-start rounded-full border border-white/10 px-3 py-1 text-xs capitalize text-slate-300">{p.purchaseStatus.replaceAll('_',' ')}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Line label="Payment" value={p.paymentStatus}/><Line label="Balloting" value={p.ballotingStatus}/></div></article>)}</div>}</section>}
function Line({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-white/10 bg-white/[0.025] p-3"><p className="text-[10px] uppercase tracking-wider text-slate-600">{label}</p><p className="mt-1 text-sm font-semibold capitalize text-slate-300">{value.replaceAll('_',' ')}</p></div>}
