import {FileCheck2,Network,ShoppingBag,Users} from "lucide-react";
type Props={directReferrals:number;totalPurchases:number;eligiblePurchases:number;kycStatus:string};
export default function DashboardStats({directReferrals,totalPurchases,eligiblePurchases,kycStatus}:Props){return <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card icon={Users} label="Direct Referrals" value={String(directReferrals)} note="Approved members using your code" c="bg-indigo-500/15 text-indigo-300"/><Card icon={ShoppingBag} label="Confirmed Purchases" value={String(totalPurchases)} note="Your active and completed purchases" c="bg-emerald-500/15 text-emerald-300"/><Card icon={Network} label="Ballot Eligible" value={String(eligiblePurchases)} note="Purchase entries ready for balloting" c="bg-amber-500/15 text-amber-300"/><Card icon={FileCheck2} label="KYC Status" value={kycStatus.replaceAll('_',' ')} note="Identity verification status" c="bg-violet-500/15 text-violet-300"/></section>}
function Card({icon:Icon,label,value,note,c}:{icon:typeof Users;label:string;value:string;note:string;c:string}){return <article className="rounded-3xl border border-white/10 bg-white/[0.035] p-5"><div className="flex items-center justify-between"><div className={`flex h-11 w-11 items-center justify-center rounded-2xl ${c}`}><Icon className="h-5 w-5"/></div><span className="text-xs text-slate-500">{label}</span></div><p className="mt-5 text-2xl font-black capitalize">{value}</p><p className="mt-1 text-sm leading-6 text-slate-500">{note}</p></article>}
