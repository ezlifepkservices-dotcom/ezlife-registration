"use client";
import {Copy,Link2,ShieldCheck,UserRound} from "lucide-react";
import {toast} from "sonner";
type Props={fullName:string;email:string;memberId:string;referralCode:string;status:string};
export default function MemberProfileCard({fullName,email,memberId,referralCode,status}:Props){const referralLink=typeof window==='undefined'?`/register?ref=${referralCode}`:`${window.location.origin}/register?ref=${referralCode}`;async function copy(v:string,l:string){try{await navigator.clipboard.writeText(v);toast.success(`${l} copied.`)}catch{toast.error(`${l} copy nahi ho saka.`)}}return <section className="rounded-3xl border border-white/10 bg-white/[0.035] p-6"><div className="flex items-start justify-between"><div><h3 className="text-lg font-bold">Membership Details</h3><p className="mt-1 text-sm text-slate-500">Your account and referral information</p></div><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-indigo-500/15 text-indigo-300"><UserRound className="h-5 w-5"/></div></div><div className="mt-6 space-y-5"><Detail label="Full Name" value={fullName}/><Detail label="Email" value={email} breakAll/><div><p className="text-xs uppercase tracking-wider text-slate-600">Account Status</p><div className="mt-2 inline-flex items-center gap-2 rounded-full border border-emerald-400/20 bg-emerald-400/10 px-3 py-1.5 text-sm font-semibold capitalize text-emerald-300"><ShieldCheck className="h-4 w-4"/>{status}</div></div><Detail label="Member ID" value={memberId} breakAll/><div><p className="text-xs uppercase tracking-wider text-slate-600">Referral Code</p><div className="mt-2 flex items-center gap-2"><code className="min-w-0 flex-1 truncate rounded-xl border border-white/10 bg-slate-950/50 px-3 py-2 font-semibold text-violet-200">{referralCode}</code><button type="button" onClick={()=>copy(referralCode,'Referral code')} className="flex h-10 w-10 items-center justify-center rounded-xl border border-white/10"><Copy className="h-4 w-4"/></button></div></div><div><p className="text-xs uppercase tracking-wider text-slate-600">Referral Link</p><div className="mt-2 flex items-center gap-2"><div className="min-w-0 flex-1 rounded-xl border border-white/10 bg-slate-950/50 px-3 py-2"><div className="flex items-center gap-2"><Link2 className="h-4 w-4 shrink-0 text-violet-300"/><span className="truncate text-sm text-slate-300">{referralLink}</span></div></div><button type="button" onClick={()=>copy(referralLink,'Referral link')} className="flex h-10 w-10 items-center justify-center rounded-xl border border-white/10"><Copy className="h-4 w-4"/></button></div></div></div></section>}
function Detail({label,value,breakAll=false}:{label:string;value:string;breakAll?:boolean}){return <div><p className="text-xs uppercase tracking-wider text-slate-600">{label}</p><p className={`mt-1 font-medium ${breakAll?'break-all':''}`}>{value}</p></div>}
